CREATE TABLE Enrollment (ENROLLMENTID INTEGER NOT NULL, ENROLLMENTCENTERNAME VARCHAR(255), PRIMARY KEY (ENROLLMENTID))
CREATE TABLE Person (PERSONID INTEGER NOT NULL, ADDRESS VARCHAR(255), AGE VARCHAR(255), FIRSTNAME VARCHAR(255), LASTNAME VARCHAR(255), ENROLLMENTCENTER_ENROLLMENTID INTEGER, PRIMARY KEY (PERSONID))
CREATE TABLE Enrollment_PERSONID (Enrollment_ENROLLMENTID INTEGER, PERSONID VARCHAR(255))
ALTER TABLE Person ADD CONSTRAINT FK_Person_ENROLLMENTCENTER_ENROLLMENTID FOREIGN KEY (ENROLLMENTCENTER_ENROLLMENTID) REFERENCES Enrollment (ENROLLMENTID)
ALTER TABLE Enrollment_PERSONID ADD CONSTRAINT FK_Enrollment_PERSONID_Enrollment_ENROLLMENTID FOREIGN KEY (Enrollment_ENROLLMENTID) REFERENCES Enrollment (ENROLLMENTID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
