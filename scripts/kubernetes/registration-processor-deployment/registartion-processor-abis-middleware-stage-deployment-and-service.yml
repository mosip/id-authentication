---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: registartion-processor-abis-middleware-stage
spec:
  replicas: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: registartion-processor-abis-middleware-stage
        component: service-hazelcast-server
    spec:
      containers:
      - name: registartion-processor-abis-middleware-stage
        image: docker-registry.mosip.io:5000/registartion-processor-abis-middleware-stage
        ports:
          - name: eventbus-port
            containerPort: 5888
        ports:
          - name: hazelcast-port
            containerPort: 5701
        env:
        - name: active_profile_env
          value: dev
        - name: spring_config_label_env
          value: 0.11.0
        - name: spring_config_url_env
          value: http://kernel-config-server:51000/config
        imagePullPolicy: Always
      imagePullSecrets:
      - name: pvt-reg-cred

