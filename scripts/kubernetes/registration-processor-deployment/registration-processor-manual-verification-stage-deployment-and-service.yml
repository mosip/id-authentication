---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: registration-processor-manual-verification-stage
spec:
  replicas: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1 
  template:
    metadata:
      labels:
        app: registration-processor-manual-verification-stage
        component: service-hazelcast-server
    spec:
      containers:
      - name: registration-processor-manual-verification-stage
        image: docker-registry.mosip.io:5000/manual-verification-stage
        ports:
          - name: eventbus-port
            containerPort: 5720
        ports:
          - name: hazelcast-port
            containerPort: 5701
        env:
        - name: active_profile_env
          value: dev
        - name: spring_config_label_env
          value: 0.9.0
        - name: spring_config_url_env
          value: http://104.211.212.28:51000
        imagePullPolicy: Always
      imagePullSecrets:
      - name: pvt-reg-cred

