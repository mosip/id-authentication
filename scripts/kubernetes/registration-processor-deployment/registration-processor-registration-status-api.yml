---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: registration-processor-registration-status-api
spec:
  replicas: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1 
  template:
    metadata:
      labels:
        app: registration-processor-registration-status-api
    spec:
      containers:
      - name: registration-status-api
        image: docker-registry.mosip.io:5000/registration-status-api
        env:
        - name: active_profile_env
          value: dev-k8
        ports:
        - containerPort: 8083
        imagePullPolicy: Always
      imagePullSecrets:
      - name: pvt-reg-cred
---
apiVersion: v1
kind: Service
metadata:
  name: registration-processor-registration-status-api
spec:
  ports:
    - port: 8083
  selector:
    app: registration-processor-registration-status-api

