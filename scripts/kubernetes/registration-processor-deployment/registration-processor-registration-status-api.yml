---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: registration-processor-registration-status-service
spec:
  replicas: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1 
  template:
    metadata:
      labels:
        app: registration-processor-registration-status-service
    spec:
      containers:
      - name: registration-status-service
        image: docker-registry.mosip.io:5000/registration-status-service
        env:
        - name: active_profile_env
          value: dev
        ports:
        - containerPort: 8083
        imagePullPolicy: Always
      imagePullSecrets:
      - name: pvt-reg-cred
---
apiVersion: v1
kind: Service
metadata:
  name: registration-processor-registration-status-service
spec:
  ports:
    - port: 8083
  selector:
    app: registration-processor-registration-status-service

