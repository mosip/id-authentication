---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: kernel-ldap-service
spec:
  replicas: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1 
  template:
    metadata:
      labels:
        app: kernel-ldap-service
    spec:
      containers:
      - name: kernel-ldap-service
        image: docker-registry.mosip.io:5000/kernel-ldap-service
        env:
        - name: active_profile_env
          value: dev
        ports:
        - containerPort: 8092
        imagePullPolicy: Always
      imagePullSecrets:
      - name: pvt-reg-cred
---
apiVersion: v1
kind: Service
metadata:
  name: kernel-ldap-service
spec:
  ports:
    - port: 8092
  selector:
    app: kernel-ldap-service

