---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: kernel-uingenerator-service
spec:
  replicas: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1 
  template:
    metadata:
      labels:
        app: kernel-uingenerator-service
    spec:
      containers:
      - name: kernel-uingenerator-service
        image: docker-registry.mosip.io:5000/kernel-uingenerator-service
        env:
        - name: active_profile_env
          value: dev-k8
        ports:
        - containerPort: 8080
        imagePullPolicy: Always
      imagePullSecrets:
      - name: pvt-reg-cred
---
apiVersion: v1
kind: Service
metadata:
  name: kernel-uingenerator-service
spec:
  ports:
    - port: 8080
  selector:
    app: kernel-uingenerator-service
