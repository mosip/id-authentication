package org.mosip.registration.processor.status.code;

/**
 * The Enum AuditLogTempConstant.
 */
public enum AuditLogTempConstant {

	/** The application id. */
	APPLICATION_ID,
	/** The application name. */
	APPLICATION_NAME,
	/** The created by. */
	CREATED_BY,
	/** The event id. */
	EVENT_ID,
	/** The event name. */
	EVENT_NAME,
	/** The event type. */
	EVENT_TYPE,
	/** The host ip. */
	HOST_IP,
	/** The host name. */
	HOST_NAME,
	/** The id. */
	ID,
	/** The id type. */
	ID_TYPE,
	/** The module id. */
	MODULE_ID,
	/** The module name. */
	MODULE_NAME,
	/** The session user id. */
	SESSION_USER_ID,
	/** The session user name. */
	SESSION_USER_NAME
}
