{
		"demographics": {
			"name": [
      {{#each name}}
            {
            "language": "{{language}}",
            "value": "{{value}}"
        }
          {{#unless @last}},{{/unless}}
      	{{/each}}
			]
		},
		"timestamp": "{{requestTime}}",
		"transactionID": "{{transactionId}}"
}
