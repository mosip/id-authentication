spring.cloud.config.uri=http://104.211.212.28:51000
spring.cloud.config.label=master
spring.cloud.config.name=resident,application
spring.application.name=resident
spring.profiles.active=int

management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=info,health,refresh
server.port=8099
server.servlet.path=/resident/v1

health.config.enabled=false


#--------------ID Auth internal --------------
auth.internal.id=mosip.identity.auth.internal
auth.internal.version=1.0


#-------------TOKEN GENERATION----------------
#Token generation request id
token.request.id=io.mosip.resident
#Token generation app id
resident.appid=registrationprocessor
#Token generation version
token.request.version=1.0
# Kernel auth client ID for IDA
resident.clientId=regproc_app_user
# Kernel auth secret key for IDA
resident.secretKey=80565e06e382d5e8b924123feb6ef43b

#Base url
mosip.base.url=https://int.mosip.io

#Auth Services
KERNELAUTHMANAGER=${mosip.base.url}/v1/authmanager/authenticate/clientidsecretkey
INTERNALAUTH=${mosip.base.url}/idauthentication/v1/internal/auth
KERNELENCRYPTIONSERVICE=${mosip.base.url}/v1/keymanager/publickey
