name: "CodeQL"

on:
  push:
    branches: [ "master", "1.0.9", "1.1.2", "1.1.3", "1.1.4", "1.1.5", "1.1.5.3", "1.1.5.4", "1.1.5.5", "1.2.0-prerel", "1.2.0-rc1", "develop", "release-1.2.0", "release-1.2.0.1" ]
  pull_request:
    branches: [ "master" ]
  schedule:
    - cron: "52 7 * * 6"

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ java ]

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v2
        with:
          languages: ${{ matrix.language }}
          queries: +security-and-quality

      - name: Autobuild
        uses: github/codeql-action/autobuild@v2

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v2
        with:
          category: "/language:${{ matrix.language }}"
