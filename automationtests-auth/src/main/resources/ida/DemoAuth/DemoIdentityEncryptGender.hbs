{
		"demographics": {
			"gender": [
      {{#each gender}}
            {
            "language": "{{language}}",
            "value": "{{value}}"
        }
          {{#unless @last}},{{/unless}}
      	{{/each}}
			]
		},
		"timestamp": "{{requestTime}}",
		"transactionID": "{{transactionId}}"
		
}
