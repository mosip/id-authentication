{
    "response": {
        "bindingAuthStatus": "{{bindingAuthStatus}}"{{#if identityCertificate}},
        "identityCertificate": "$IGNORE$"{{/if}}{{#if authToken}},
        "authToken": "$IGNORE$"{{/if}}
    }
}
