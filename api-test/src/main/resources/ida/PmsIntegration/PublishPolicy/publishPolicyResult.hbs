{
    "response": {
        "policyType": "{{policyType}}",
        "is_Active": "{{is_Active}}",
        "policies": {
            "allowedAuthTypes": [
                {
                    "authType": "{{authType}}"
                }
            ],
            "allowedKycAttributes": [
                {
                    "attributeName": "{{attributeName}}"
                }
            ],
            "authTokenType": "{{authTokenType}}"
        }
    }
}