{
    "response": {
        "authTransactions": [
            {{#each authTransactions}}
      {
        "transactionID": "{{code}}",
		"referenceIdType": "{{referenceIdType}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}},
        ]
    },
}