{
    "errors": [
      {{#each errors}}
        {
            "errorCode": "{{errorCode}}"
        }
          {{#unless @last}},{{/unless}}
      	{{/each}}
  ],
    "response": {
        "kycStatus": {{response.kycStatus}}
    }
}