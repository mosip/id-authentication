{
    "response": {
        "kycStatus": "{{kycStatus}}"{{#if kycToken}},
        "kycToken": "{{kycToken}}"{{/if}}{{#if authToken}},
        "authToken": "{{authToken}}"{{/if}}{{#if verifiedClaimsMetadata}},
        "verifiedClaimsMetadata": "{{verifiedClaimsMetadata}}"{{/if}}
    }
}
