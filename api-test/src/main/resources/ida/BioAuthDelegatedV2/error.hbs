{
    "errors": [
      {{#each errors}}
        {
            "errorCode": "{{errorCode}}"
        }
          {{#unless @last}},{{/unless}}
      	{{/each}}
  ],
    "response": {
        "authStatus": {{response.authStatus}}
    }
}