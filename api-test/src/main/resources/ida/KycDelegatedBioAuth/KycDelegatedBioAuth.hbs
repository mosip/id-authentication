{
    "biometrics": [
        {
            "data": {
                "bioSubType": "{{bioSubType}}",
                "bioType": "{{bioType}}",
                "bioValue": "{{bioValue}}" ,
                "deviceCode": "{{deviceCode}}",
                "deviceProviderID": "{{deviceProviderID}}",
                "deviceServiceID": "{{deviceServiceID}}",
                "deviceServiceVersion": "{{deviceServiceVersion}}",
                "env": "Staging",
                "domainUri": "https://api-internal.dev-int.mosip.net",
                "digitalId": {
                    "serialNo": "{{serialNo}}",
                    "make": "{{make}}",
                    "model": "{{model}}",
                    "type": "{{type}}",
                    "deviceSubType": "{{deviceSubType}}",
                    "deviceProvider": "{{deviceProvider}}",
                    "deviceProviderId": "{{deviceProviderId}}",
                    "dateTime": "{{dateTime}}"
                },
                "mosipProcess": "Auth",
                "purpose": "Auth",
                "qualityScore": 70,
                "requestedScore": 70,
                "timestamp": "{{timestamp}}",
                "transactionId": "{{transactionId}}"
            }
        }
    ],
    "timestamp": "{{timestamp}}"
}